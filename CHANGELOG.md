# 更新日志

本文档记录 Code Agent 插件的版本迭代内容。遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 与语义化版本规范。

## [0.9.0] - 2026-08-23

### 对话模式权限体系修正（P0）

- **对话模式只读文件问答能力**：重新定义两种运行模式的权限边界——对话模式从「纯文本问答」升级为「只读文件问答」，完整开放只读类文件操作能力（文件内容读取、目录结构遍历、代码检索、diff 对比），支持读取工作区目录内所有文件，同时支持读取工作区外的本地文件与目录（含 @ 引用、右键注入、文件夹批量引用），满足基于项目文件与本地文件的问答需求。
- **调度层权限管控**：对话模式仅挂载只读工具（read_file / list_dir / search_code / get_diff），文件写入、终端执行等工具不入请求，从 Agent 调度层面屏蔽无效越权调用。
- **执行层统一权限校验**：工具执行前置链路新增统一权限校验模块（checkToolAllowed），基于当前运行模式判定操作合法性，所有工具调用必经该校验层；对话模式下任何写入/终端类操作均在执行前拦截并返回明确提示：「对话模式仅支持只读文件问答，如需修改文件或执行命令，请切换至智能体模式」，越权调用同步写入操作审计日志。
- **模式约束系统提示**：对话模式下向模型注入能力边界约束，明确告知仅只读工具可用、写入与命令操作会被系统拒绝，引导模型在需要修改代码或执行命令时提示用户切换模式。
- **智能体模式读取权限完整开放**：文件读取权限统一为「完整开放」——任何运行模式下均可读取工作区内所有文件及工作区外的本地文件与目录（文件读取、目录遍历、代码检索、diff 对比），工作区外读取不再按策略询问/拒绝（历史「工作区外文件策略」配置项随之废弃移除）。
- **询问/全自动模式作用范围收窄**：询问模式与全自动模式的区分仅作用于智能体模式下工作区内文件的修改/编辑/新增/删除；终端命令执行不再受该模式影响，仅由「终端免确认」开关与高危命令拦截规则控制。
- **工作区外写入禁令不变**：工作区外文件在任何运行模式下依旧只读——可读取、不可修改/删除，写入操作执行前拦截。
- **模式切换即时生效**：模式随每次请求实时传递，权限规则同步切换，无缓存残留。

### 核心功能全量回归校验（P0）

- 智能体模式完整执行链路、上下文管理与 Token 统计、历史会话读写、编辑器高亮联动、全局设置项配置、多模型切换、安全权限模式切换、对话滚动交互等核心功能全部回归通过，权限修改仅作用于对话模式能力边界，无功能回归。

## [0.8.0] - 2026-08-22

### 对话布局体系重构（P0）

- **双层滚动体系**：对话面板全局滚动 + 内容子块二级滚动——思考过程、工具执行结果、终端输出、工具参数等所有独立内容子块统一最大高度阈值（260px），超出后区块内部出现独立滚动条，长内容不再无限制撑高对话面板；子块滚动条样式对齐 VSCode 原生滚动条风格。
- **区块统一折叠/展开**：所有内容子块头部栏新增折叠切换按钮，可收起为单行标题条目（如「思考过程」「read_file · 执行成功」），默认展开；折叠不影响流式内容更新，折叠后形成清晰的执行链路条目列表，便于宏观把控完整任务流程。
- **滚动交互兼容**：子块内部滚动不触发全局滚动的自动归位逻辑，与 V0.7.0 智能滚动无交互冲突。

### 推理链路分层展示（P0）

- **推理节点（思维链主干）**：新增 insight 分段——工具调用轮次的模型正文（决策结论、动作说明、结果小结）作为轻量主干节点分散挂载到对应执行节点前后，工具调用出现时自动从回复气泡迁移；仅最终回复轮次的正文保留在正式回复气泡，收尾体验不变。
- **深度思考二级子块**：完整推理过程（reasoning_content）作为可折叠、可内部滚动的独立子块，挂载在对应推理节点附近，与主干节点视觉分层；严格遵循「推理节点 → 深度思考 → 工具调用执行 → 执行结果小结」时序结构，推理逻辑与每一步执行动作一一对应，完整思维链无缺失、不堆积。

## [0.7.0] - 2026-08-22

### 交互体验优化（P0）

- **对话滚动智能跟随**：重构输出过程中的滚动控制逻辑——用户手动拖动滚动条或滚动滚轮立即暂停自动跟随（视口稳定停留在当前位置，新内容不再强制拉回底部）；停止滚动操作 5 秒后自动平滑滚动至最新输出位置恢复实时进度展示，倒计时内再次滚动则重置计时；无用户干预时保持默认跟随，始终展示最新执行节点。
- **思考链分散分段排版**：思考区块内部按语义单元（空行）自动分段排版，段落间距与行高标准化，移除思考区内部滚动高度限制改为完整展开展示；所有分散思考区块沿用统一视觉样式，长思考内容支持独立展开/折叠（默认完整展开）。配合 V0.6.0 时序化分段结构，推理逻辑与每一步执行动作一一对应，任务结束后可完整回溯。

## [0.6.0] - 2026-08-22

### 交互范式重构（P0）

- **流式分步执行输出**：对话流渲染改为时序化分段结构——思考内容分段流式输出（每轮推理独立成段，可展开/折叠，默认展开），工具调用解析完整即插入工具执行节点（loading 态即时可见），执行结果完成后原位更新为结果节点；「思考→调用工具→返回结果→继续思考→调用下一工具」按实际执行时序逐段交替展示，任务结束后保留完整分步执行链路可回溯。旧历史消息自动回退原渲染方式，无兼容问题。

### 数据实时性优化（P1）

- **上下文 Token 实时统计**：每接收一段流式输出（思考/回复/工具结果）后节流（500ms）重算会话完整消息链 Token 占用，未落盘的流式内容按启发式估算补充计入，面板顶部进度条与数值随输出实时更新，无整轮结束才刷新的滞后。
- **今日用量与调用次数实时累加**：每次模型接口响应（每一轮流式调用）返回后立即累加今日 Token 用量、消费金额与 API 调用次数，与接口计费数据对齐；多轮任务执行过程中数据实时可查，杜绝收尾一次性补记的滞后。
- **账户余额全量字段展示**：余额查询全量映射官方接口字段（总余额/充值余额/赠金余额），并拼接本地已用金额与今日调用次数；设置面板打开时自动拉取最新用量数据，「查询账户余额」按钮支持手动刷新。

### 编辑器高亮联动修复（P1）

- **高亮样式精简**：新增行装饰移除 border 外边框，改为左侧 gutter 绿色竖线 + 浅绿行背景；删除行改为间隙前一行左侧红色竖线 + 浅红行背景（新增与删除重合时新增优先），对齐 VSCode 原生 Diff 视觉体验。
- **跳转联动即时生效**：点击对话框文件变更节点「跳转编辑器」打开文件时，自动重新应用最近一次变更的高亮装饰，打开即可看到增删行标记，无需关闭重开。
- **标记生命周期规范化**：同一文件再次修改时自动清除旧装饰刷新为最新变更；用户手动关闭文件后装饰类型释放，重新打开不再残留标记（仅通过跳转按钮打开时恢复展示）。

## [0.5.1] - 2026-08-22

### 缺陷修复（P0）

- **历史会话删除无响应修复**：删除操作改用两段式内联确认——点击「删除」后按钮变为「再点一次确认」，再次点击才真正删除，5 秒内未确认自动还原；修复根因为 VSCode webview 禁用原生 window.confirm（调用恒返回 undefined），原实现依赖其返回值判断导致删除消息从未发出。菜单关闭时同步清除确认态，避免状态残留。

## [0.5.0] - 2026-08-22

### 缺陷修复（P0）

- **会话列表空白修复**：修复历史会话元数据截断解析的尾随逗号问题（截断到 messages 字段前时上一字段逗号未移除，JSON 解析抛错），导致全部会话被误判损坏、侧边栏列表为空，新建会话也因文件监听触发的全量重扫而消失；修复后启动即恢复全部历史会话，新建会话实时入列。
- **账户已用金额统计修复**：重新实现已用金额口径——余额接口仅返回余额快照（赠金优先扣费场景下「充值+赠金−当前余额」恒为 0，无法推导累计消费），改为本地累计：每次模型调用完成后按「模型单价 × Token 用量」估算消费金额（DeepSeek 官方公开标准价表，自然日口径，持久化到本地用量日志）；设置页展示恢复为「余额 X.XX CNY（已用 Y.YY CNY）」，数值随调用消耗实时累加，接口异常仍展示「查询失败」兜底。
- **Token 统计口径修正**：无校准快照时（新会话/压缩后/重启后）的启发式估算中，CJK 字符由 1 token/字符修正为约 0.6 token/字符（对齐 DeepSeek 中文分词实测量级），避免中文会话上下文占用高估 60% 以上；压缩与重新生成后主动清除旧校准快照，防止链长重新增长后误用旧基准高估占用。

### 体验优化（P1）

- **会话列表虚拟滚动修复**：抽屉收起态挂载后展开时未建立高度测量的问题修复，展开后可视窗口正常计算（此前退化为单行窗口）；长会话列表滚动恢复流畅。
- **外部点击收起交互修正**：收起例外选择器与侧边栏实际类名对齐，展开态下侧边栏内部点击不会误触发收起。

## [0.4.0] - 2026-08-22

### 性能优化（P0，执行效能专项落地）

- **并行工具调用**：同批次工具调用按依赖关系分组调度——连续只读工具（read_file/list_dir/search_code/get_diff）合并并行执行，连续 write_file 目标路径互不相同可并行，execute_command 恒串行保持命令语义与顺序依赖；多文件读取、目录检索等批处理场景工具执行段由串行叠加降为单次最大耗时。
- **流式执行流水线**：模型流式输出过程中，单个工具调用参数接收完整（JSON 闭合可解析）即预启动只读工具执行，推理/思考生成与工具执行并行重叠；流结束后直接复用预执行结果（未完成则等待，不重复执行），最终未生效的调用结果自然丢弃。
- **上下文动态裁剪**：距离消息链末尾超过 16 条且内容超 4000 字符的历史工具输出，在发送前轻量降级为「头部结论 + 省略标记 + 尾部细节」（保留文件头/命令头与错误信息等关键信息）；仅发送时降级，本地持久化完整原文不变，保障回溯与自我修正能力。
- **渲染层增量更新**：MessageBubble 与 StepBlock 组件 memo 化（消息引用/步骤引用不变时跳过重渲染），流式期间仅目标消息与目标步骤更新，长会话高频更新时 UI 线程占用下降。

## [0.3.0] - 2026-08-22

### 缺陷修复（P0）

- **Token 统计真实化**：移除文件大小/词元计数等近似估算，改用模型返回的真实 `prompt_tokens` 值校准（消息链长度快照对齐统计口径，链收缩时回退启发式估算）；顶部栏展示统一为「已用 X.XK / 总窗口 Y.YK」格式，会话 Token 统计与用量上报口径一致。
- **账户已用金额修复**：DeepSeek 余额接口无直接「已用」字段，改为差额法计算（已用 = 充值 + 赠送 − 总余额），修复已用金额被误映射为赠送余额的问题；接口异常时展示独立错误提示，不再显示错误金额。
- **历史会话持久化加载异常修复**：激活 + WebView 就绪后强制全量扫描；严格匹配会话文件命名过滤日志文件（audit-log.jsonl / usage-log.json 等不再误入列表）；仅解析元数据不全量加载；单文件损坏记录错误日志并跳过；新增历史目录变更监听（防抖 300ms）实时同步列表。

### 功能增强（P1）

- **编辑器 diff 装饰与跳转联动**：文件修改完成后自动在编辑器内高亮变更行（新增行绿色背景 + 删除行红色竖条），关闭文件自动清除装饰；对话框内 diff 全量渲染改为「文件名 + 变更统计」精简节点，点击跳转编辑器定位首处变更。
- **编辑器右键选中代码注入**：编辑器右键菜单新增「Add to Code Agent」（有选区时可用），将选中代码以行范围引用注入当前对话输入框（展示 `文件名 起始行-结束行` 标签），仅将对应行号范围的代码加载至上下文，与 @ 全文件引用复用同一套上下文管理机制；面板未就绪时请求暂存，就绪后自动下发。
- **思考语言自适应**：系统提示词按用户提问语言动态约束思考过程与回复语言（中文提问→中文思考，英文提问→英文回复），保留技术术语原文。
- **工具调用最大轮次可配置**：设置页「Agent 行为」分组新增「最大工具调用轮次」数值项（正整数 1-1000，默认 20，非法输入自动回退合法值）；配置即时生效（新建与续接会话无需重启）；触发上限时提示文案展示当前配置值并引导调整入口。

### 体验优化（P1）

- **会话侧边栏点击外部自动收起**：侧边栏展开时点击对话主面板空白/内容区域自动收起（输入框、按钮等交互控件不触发），收起动画与原有收起按钮一致，布局状态同步持久化。

## [0.2.0] - 2026-08-21

### 缺陷修复（P0）

- **上下文压缩消息链非法导致 400 错误**：压缩切分边界自动对齐（保留段开头不再出现 tool 消息），发送前增加消息链合法性前置校验（剔除孤立 tool 消息、未完整配对的 tool_calls 整体降级为占位文本），杜绝压缩后请求被 API 拒绝。
- **WebView 冷启动偶发空白/无响应**：新增 ready 握手与 boot:ack 就绪确认机制；渲染失败自动重试 1 次；超时展示错误提示界面并提供手动刷新入口；设置面板与权限弹窗改为按需懒加载，显著降低冷启动开销。

### 功能增强（P1）

- **会话列表体验**：
  - 虚拟滚动：100+ 会话列表流畅滚动；
  - 右键菜单标准化：菜单位置对齐鼠标坐标，点击外部/ESC 关闭，重命名失焦自动提交；
  - 抽屉式展开/收起：展开不挤压对话区域，右侧拖拽调宽（180-480px），收起无残留占位，布局状态持久化。
- **思考过程展示**：DeepSeek 推理模型 `reasoning_content` 全链路流式展示；思考区块视觉弱化，生成中自动展开、完成后默认折叠，内容永久保留可回溯。
- **Markdown 渲染增强**：完整表格渲染；代码块语法高亮（12+ 语言）、行号、一键复制；富文本复制（剪贴板同时写入纯文本与 Markdown 源文本，粘贴到编辑器保留格式）。
- **权限模式快捷切换**：底部栏新增询问/全自动模式切换器，即时生效并与全局设置双向同步；全自动模式仅放行常规操作（工作区内文件写入、工作区外读取、工作区内命令），高危命令始终强制二次确认。
- **模型参数语义明确化**：模型元数据（上下文窗口 / 最大输出 Token = 模型能力上限，用于边界校验与用量统计）与全局推理参数 `max_tokens`（单次请求生成长度限制 = 运行时参数）tooltip 区分；设置面板实时校验：`max_tokens` 超过所选模型输出上限时警示并可一键修正，保存与请求阶段均自动截断为上限值；模型编辑校验输出上限不得超过上下文窗口。
- **API Key 安全展示**：密文显示 + 显隐切换按钮 + 实时配置状态反馈（已配置/未配置/待保存）。

### 性能优化（P1）

- **对话区虚拟滚动**：仅渲染视口内消息区块（动态高度测量 + 窗口切片 + 预估高度），流式生成自动跟随滚动；百轮长会话打开、滚动、发送均流畅，DOM 节点数量恒定于视口规模。
- **长内容默认折叠**：超长代码块（>30 行）与超长纯文本（>4000 字符）默认折叠，点击展开全文，避免一次性渲染大量 DOM 节点。
- **数据层惰性加载**：会话列表仅读取文件头部元数据（截断解析，不解析消息体）；完整消息体在打开会话时按需读取；内存缓存 LRU 上限 8 个会话，非激活数据不常驻内存；列表广播走内存元数据缓存，零磁盘 IO。
- **通信层消息合并**：流式 chunk 与思考增量 16ms 缓冲批量下发，显著降低高频 postMessage 对主线程的阻塞。

## [0.1.0]

- 首个正式版本：多模型接入（OpenAI 兼容协议）、智能编程 Agent（任务规划/工具调度/多轮闭环）、四层上下文管理与自动压缩、@ 文件引用、会话管理、安全权限体系与操作审计。
