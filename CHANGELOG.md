# 更新日志

本文档记录 Code Agent 插件的版本迭代内容。遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 与语义化版本规范。

## [1.4.5] - 2026-08-24

### 多模态能力标识读取层缺陷修复（P0）

- **模型缓存读取 multimodal 字段丢失根因修复**：`getModels()` 规范化模型缓存时遗漏透传 `multimodal` 字段，导致预置标记 / 拉取探测 / 手动校准的多模态标识「写入有效、读取丢失」——发送前双层校验恒判「不支持图片输入」，Kimi K3 等多模态模型被误拦截（图片链路本身已按 Base64 Data URL 协议内嵌，符合 Kimi 等服务商「图片必须 base64、不接受公网 URL」的约束）。现布尔值原样透传：显式 false 视为用户校准（重新拉取时优先保留），缺省/非法值视为不支持；修复后月之暗面 / DeepSeek / 智谱 / 通义 / 豆包 / OpenAI / Anthropic 等全服务商多模态模型均可正常图片输入。
- **校准弹层状态持久化修复**：设置页「模型元数据校准」的「支持图片输入」复选框保存后重开不勾选的问题（与读取层字段丢失同根因）修复，保存的校准值正确回显；预置标记 / 动态探测命中的视觉模型默认勾选。
- **手动添加模型默认校准**：手动添加模型且能力标识未设置时，命中已知视觉模型 ID 模式自动勾选（与动态拉取探测同口径），用户显式勾选/取消原样尊重。

### 模型适配与交互优化

- **DeepSeek 视觉模型适配**：预置厂商目录新增官方视觉模型 `deepseek-v4-flash-vision-exp`（DeepSeek V4 Flash Vision Exp）并标记多模态；已知视觉模型 ID 模式的通用 vision 标记覆盖 DeepSeek 视觉系，动态拉取自动识别。
- **校准弹层文案优化**：「多模态能力」字段标签去除版本号后缀（避免后续迭代后版本号过时），提示文案同步明确「默认勾选 + 手动校准」语义。

### 版本与文档迭代（P0）

- 插件版本号 1.4.0 → 1.4.5（package.json / package-lock.json 同步），CHANGELOG / README 更新，作者署名 wangh 保持不变。
- 回归验证：typecheck + build 全绿；多模态标识读取透传、校准合并语义、视觉模型 ID 模式正/反例矩阵经脚本化功能测试逐项通过；存量纯文本对话 / 智能体链路零负向优化。

## [1.4.0] - 2026-08-24

### 多模态（图片）输入能力体系

- **三路径图片输入**：对话输入框支持剪贴板粘贴、文件拖拽、`@` 文件引用三种方式插入图片；插入后以缩略图内联预览，支持单张删除与一键批量清空（不影响已输入文本与附件）；格式限定 PNG/JPG/JPEG/WebP、单张不超过 10MB、单条消息至多 10 张，超限均给出明确文案提示并拒绝插入。
- **OpenAI 兼容多模态协议适配**：带图用户消息按协议组装为 content 内容块数组（text 块 + image_url 块按序排列），本地图片以 Base64 Data URL 内嵌；组装逻辑下沉至模型适配器统一层（流式/非流式共用），纯文本消息序列化结果与历史版本逐字节一致，零负优化。
- **图片生命周期完整闭环**：图片随用户消息持久化至会话历史，历史回显、会话续接、重新生成均原样复用；上下文压缩时仅以「(附带 N 张图片)」标注降级，Token 按启发式估算并随服务商返回的真实 `prompt_tokens` 校准。

### 模型多模态能力标识与校验拦截

- **multimodal 能力标识**：模型元数据新增多模态标识字段——预置目录按官方能力标记（GPT-4o 系列、Claude 3.5 Sonnet/Opus 等支持视觉的模型为 true，纯文本模型不标记）；`/models` 动态拉取按 capabilities/modalities/supports_vision/vision 四路尽力探测，未声明默认不支持；设置页模型编辑区开放手动修改入口，手动校准值在重新拉取时合并保留。
- **视觉模型探测缺口修复**：多家服务商（月之暗面/智谱/通义/豆包/OpenAI/Anthropic）官方文档声明视觉模型但 `/models` 接口不返回能力元数据，四路探测全落空导致 kimi-k3 等官方视觉模型被发送前校验误拦截；探测层新增「已知视觉模型 ID 模式」第五路兜底（策划式保守匹配：kimi-k3/kimi-k2.5+、GLM-4V 系、qwen-vl/QVQ 系、含 vision 的 ID、gpt-4o/4.1/4-turbo/5、o3 非 mini/o4 系、Claude 3+/4 系，纯文本模型明确不命中，52 正例/33 反例矩阵测试零误标）；预置目录同步补标 kimi-k3、moonshot-v1-8k-vision-preview、glm-4v-flash、qwen-vl-max、doubao-vision-pro-32k 五个官方视觉模型。
- **发送前能力校验双重拦截**：前端发送时预校验 + 扩展侧发起前最终校验，当前模型不支持图片输入时提示「当前模型「模型名称」不支持图片输入，请更换支持多模态的模型后重试」并拦截——不发起请求、不消耗 Token；拦截时输入区文本/附件/图片全部保留，切换多模态模型后可直接重发；纯文本对话完全不受影响。

### 专项代码审查加固

- 多模态链路专项代码审查（0 Critical / 2 Major / 3 Minor）并全部修复：撤除 moonshot-v1-* 纯文本模型的多模态误标（避免校验放行后服务商 400）；拦截时机前置到前端防止图片丢失与幽灵消息残留；补齐图片数量上限、`@` 路径读取图片的工作区边界收敛校验。
- 拦截语义与路径收敛专项复核再加固：拦截时文本/附件/图片零丢失、无幽灵消息残留、前后端校验口径一致均逐项验证通过；`readImageFile` 追加符号链接真实路径复核（防工作区内植入链接指向区外）、无工作区时直接拒绝引用读取、多根工作区改为任一根通过即放行。
- 全量回归核验通过：typecheck + build 全绿，存量纯文本对话/智能体链路请求体不变，零负向优化。

### 版本与文档迭代

- 插件版本号 1.3.5 → 1.4.0（package.json / package-lock.json 同步），CHANGELOG / README 更新，作者署名 wangh 保持不变。

## [1.3.5] - 2026-08-24

### 限流退避等待可视化

- **429 退避等待实时提示**：1.3.3/1.3.4 的限流退避重试为静默等待（最长约 10 秒），用户侧无任何反馈、易误判为卡死；现退避开始时适配器回调上层，对话区底部弹出中性色提示「服务商限流（429），N 秒后自动重试（第 X 次），请稍候…」；流式内容恢复/结束/报错/新一轮对话时自动消失，另有 15 秒兜底自动消失；提示不写入会话历史，不影响正常流程。

## [1.3.4] - 2026-08-24

### 兼容匹配精度加固（零负优化保证）

- **top_p 误伤防护**：1.3.3 的 topp 拼写兼容采用子串匹配，可能误命中 stopped 等英文单词导致 top_p 被错误省略；改为词边界匹配（\btopp\b），杜绝子串误伤。
- **零负优化约定明确化**：兼容记忆仅在服务商明确以 400 拒绝某字段时写入；无限额/全兼容服务商（如 DeepSeek）不触发任何记忆与重试，请求体与历史版本逐字节一致；记忆按 Base URL 隔离，服务商间互不影响；429 退避重试仅在真实限流时生效。

## [1.3.3] - 2026-08-24

### 适配器通用兼容体系升级（厂商无关）

- **参数兼容记忆**：400 时将错误报文映射为不支持字段（stream_options / temperature / top_p / frequency_penalty / tool_choice），按 Base URL 记入模块级兼容记忆，后续请求主动省略——进程生命周期内仅一次试错；修复 1.3.2 「temperature 试错重试」在严格 RPM 限额下（月之暗面免费额度 org max RPM 3）使单次对话消耗双倍配额、第二次对话即触发 429 的问题。
- **429 限流退避重试**：流式与非流式链路在收到 429 时按报文提示秒数（如 try again after 1 seconds）等待后自动重试（至多 2 次），瞬时限流不再直接报错打断对话。
- 兼容覆盖主流 OpenAI 兼容服务（DeepSeek / 月之暗面 / 智谱 / 通义 / OpenAI / Ollama 等）：参数记忆 + 限流退避 + /models 匿名拉取 + Ollama 风格响应解析 + 余额接口友好降级均已厂商无关化。

## [1.3.2] - 2026-08-24

### 兼容性与体验修正

- **temperature 受限模型自动降级**：部分模型限定 temperature 取值（如月之暗面 Kimi K2 系列仅允许 temperature=1），全局推理参数非 1 时服务商返回 400（invalid temperature）。适配器层流式与非流式链路在收到 temperature 相关 400 时自动修正为 1 并重试一次，无需用户逐模型改参。
- **编辑目标服务商初始化修正**：参数编辑区「编辑目标服务商」初始值原硬编码为首个服务商（恒为 DeepSeek 预置），导致每次打开设置页重置回 DeepSeek；现初始跟随全局默认服务商，与单向联动语义对齐。

## [1.3.1] - 2026-08-23

### 阻断性缺陷热修复

- **自定义服务商新增失败根因修复**：设置页服务商提交路由误按「弹窗状态对象存在性」判定编辑/新增——新增弹窗的占位对象（id 为空）被误判为编辑，导致新增请求以「编辑空 id」发送，后端报「服务商不存在」。现改为按服务商 id 判定，新增请求正确进入新增流程（该缺陷自 V1.1.0 起遮蔽了后续各版修复效果，本次彻底根除）。
- **手动添加模型入口补齐**：参数编辑区新增「＋ 添加模型」按钮，可手动填写模型 id、展示名称、上下文窗口与最大输出（添加模式 id 可编辑、编辑模式仍只读），闭环「模型拉取失败 → 仍要创建 → 手动补模型 → 正常调用」的降级链路。

## [1.3.0] - 2026-08-23

### 核心阻断性缺陷修复（P0）

- **密钥重配置后状态同步失效修复**：对话发起前的密钥校验由旧版全局密钥改为按当前模型所属服务商实时读取系统密钥库（SecretStorage），修复预置服务商清除密钥后重新填写保存仍报「未配置 API Key」的缺陷；密钥保存后无需重启插件即时生效，配置页展示状态与实际运行状态完全一致；模型适配器按请求新建、无实例缓存，不存在陈旧密钥状态复用。
- **鉴权错误提示区分场景**：发起前拦截「未配置密钥」（提示精确到服务商名称）与运行期「密钥无效/鉴权失败」（401/403 精准提示「API Key 鉴权失败，请检查密钥有效性」）两类场景严格区分，不再状态误判。
- **自定义服务商新增链路修复**：新增环节无任何服务商白名单/厂商名匹配校验，任意合规 HTTP/HTTPS Base URL 均可接入；错误提示按场景精准拆分——Base URL 格式不合法、无法连接至接口地址、API Key 鉴权失败、接口未兼容 OpenAI 协议规范，彻底消除「服务商不存在」类笼统提示。
- **新增降级兜底（强制创建）**：模型列表拉取失败时默认回滚新增并给出精准原因；新增弹窗提供「仍要创建（手动添加模型）」选项，勾选后强制完成服务商创建，用户手动添加模型名称与元数据后即可正常调用，避免因接口兼容问题完全无法使用；回滚时同步清理密钥与同步错误状态，无残留。

### 配置页服务商语义澄清与交互优化（P1）

- **双下拉框语义澄清**：「默认服务商」标注「全局默认」标识，明确为运行时生效的全局配置（决定新建会话默认服务商与主界面默认加载的模型列表）；「模型参数配置区服务商」重命名为「编辑目标服务商」，明确为配置页编辑选择器（仅切换查看/修改模型元数据的目标服务商，不影响运行中的默认服务商与会话实际配置），两者均配说明文案。
- **布局分区**：两个下拉框拆分为「运行配置 · 全局默认」与「编辑配置 · 参数编辑区」两个视觉分组，各带分组标题，层级关系一目了然。
- **联动逻辑权责分离**：修改全局默认服务商后参数编辑区自动同步跟随，减少重复操作；编辑区切换服务商不反向修改全局默认值。

### 代码审查与全量回归（P0）

- 三条核心链路专项审查通过：密钥读写与 SecretStorage 交互（无敏感信息落盘、无陈旧缓存）、配置刷新与适配器重建（无实例缓存、时序与并发安全）、服务商新增校验与错误处理（回滚无密钥残留、预置 DeepSeek 不受影响）；审查发现的同步状态残留 Minor 问题已在发布前修复。
- 存量核心功能全量回归通过：预置 DeepSeek 对话/智能体全链路、Token 统计与压缩、历史会话管理、编辑器高亮联动与右键注入、双层权限体系、思考链展示与滚动交互等均与 V1.2.0 体验一致，零负向优化。

### 版本与文档迭代（P0）

- 版本号由 1.2.0 升级为 1.3.0，package.json 与 package-lock.json 全项目版本标识同步更新；作者署名 wangh 保持不变。
- 更新 CHANGELOG 与 README：多模型配置章节补充两个服务商配置项的语义说明与操作指引。

## [1.2.0] - 2026-08-23

### 核心阻断性缺陷修复（P0）

- **自定义服务商新增失败修复**：移除服务商新增链路的隐性限制，开放全量自定义接入能力——所有符合 OpenAI 兼容协议的接口均可通过填写 Base URL 完成新增；修复配置读写作用域不对齐导致新增后服务商不可见的缺陷（工作区级配置遮蔽全局配置时，新增服务商写入全局后读不到，误报「服务商不存在」），读写统一对齐到已定义的最高优先级作用域；拉取失败时返回精准分类错误（鉴权失败/接口不兼容/网络异常），不再笼统提示服务商不存在；预置 DeepSeek 保留不可删除、可编辑规则不变。
- **服务商密钥清除失效修复**：重构编辑保存的密钥清除专属分支——勾选「清除已配置的密钥」保存后，立即从系统密钥库彻底删除对应服务商的 API 密钥（含预置 DeepSeek 的旧版全局密钥回退源，修复此前清除后仍可正常调用的缺陷）；清除后调用该服务商模型统一拦截并提示前往设置页补充密钥；单服务商密钥清除不影响其他服务商。
- **API 密钥安全存储规范落地**：全量密钥统一存放于 VSCode 原生 SecretStorage 系统密钥库，与插件配置文件、工作区数据完全隔离；严格执行敏感信息落盘禁令——所有持久化文件（配置、历史会话 JSON、操作/审计日志）均不写入任何明文或加密形式的密钥，仅存储服务商标识、Base URL、模型元数据等非敏感信息；密钥仅运行时内存中用于接口请求，不进入打包产物与工作区目录；全量存储审计确认无密钥泄露路径。

### 预置服务商体系与模型列表兜底（P1）

- **主流厂商预置配置**：预置国内外主流 OpenAI 兼容厂商官方配置（DeepSeek、智谱 AI、月之暗面 Kimi、通义千问、字节跳动豆包、OpenAI、Anthropic），含官方标准 Base URL，作为新增时的快捷选项；Anthropic 附协议兼容性提示（原生接口非 OpenAI 兼容，需改网关地址）。
- **兜底模型列表**：所有预置服务商配套官方标准模型列表兜底数据（模型名称、上下文窗口、最大输出 Token 元数据），解决无密钥时无法拉取模型列表的问题；动态拉取优先级高于兜底数据：配置密钥后自动以接口拉取的最新列表覆盖兜底。
- **新增服务商交互优化**：「新增模型服务商」弹窗的名称与 Base URL 输入框升级为带下拉建议的复合输入框——点击 ▾ 展开预置厂商列表，选中自动填充名称与官方 Base URL，支持手动编辑自定义内容（兼容私有化部署/自定义代理）；API Key 保持手动填写不预置；无密钥新增时使用兜底模型列表并提示「未配置密钥，使用预置模型列表，配置密钥后可自动同步最新模型」。
- **主界面模型下拉框服务商联动**：底部模型切换下拉框与当前生效服务商绑定，仅展示该服务商下的模型，不同服务商分区不混杂；设置页新增「默认服务商」切换，切换后主界面模型列表实时同步；「默认模型」条目随服务商联动（不属于新服务商时自动切换为该服务商首个模型）；模型切换后上下文统计与窗口阈值同步适配对应模型元数据。

### 全量功能回归校验（P1）

- 智能体模式完整执行链路、对话模式文件问答、上下文 Token 统计与压缩、历史会话读写/续接/删除、编辑器高亮联动与右键代码注入、双层权限体系、思考链分层展示与对话滚动交互等存量核心功能全量回归通过，本次修改仅作用于模型配置与接入层，V1.0.0/V1.1.0 功能零回归，DeepSeek 存量场景正常使用不受影响。

### 代码审查健壮性加固（V1.2.0 发布前）

- **多根工作区配置写入修复**：作用域对齐写入改为逐个文件夹探测后以文件夹作用域配置对象定向写入，修复多根工作区下配置项已在文件夹层定义时写入抛错导致服务商增删改/保存设置失败的问题。
- **会话切换模型联动刷新修复**：默认服务商反向更新改为写入完成后立即广播最新设置，修复 WebView 侧停留在旧服务商导致下拉框过滤不刷新的问题。
- **预置匹配校验加固**：新增服务商弹窗从 Base URL 建议列表选择时经预置匹配校验判定 presetId，名称已被自定义时不携带预置 id，避免「自定义网关 + 官方兜底模型」错配落库。
- **默认模型回退对齐**：默认模型失效回退时优先取默认服务商名下首个模型，避免默认模型与默认服务商分属不同服务商的悬空态。
- **兜底入库保护存量**：无密钥兜底仅在该服务商无模型缓存时写入，不再覆盖用户手动添加的自定义模型；兜底未发生真实网络拉取时不记录同步时间，避免设置页展示误导性同步状态。

### 版本与文档迭代（P0）

- 版本号由 1.1.0 升级为 1.2.0（次版本迭代：核心缺陷修复 + 安全加固 + 能力扩展），package.json 与 package-lock.json 全项目版本标识同步更新。
- 更新 CHANGELOG 与 README：多模型接入章节补充预置厂商列表、自定义接入指引与密钥安全说明；作者署名 wangh 保持不变。

## [1.1.0] - 2026-08-23

### 全局默认参数统一调整（P0）

- **模型元数据默认值统一**：所有模型（含动态拉取与用户自建）的默认上下文窗口统一为 300000 Token（300.0K）、默认最大输出统一为 100000 Token（100.0K）；未返回元数据的模型接口自动套用该兜底值，边界校验与用量统计口径一致。
- **推理参数默认值提升**：全局 `max_tokens` 默认值由原值提升为 100000（上限仍受所选模型最大输出能力约束，保存与请求阶段自动截断）；「最大工具调用轮次」默认值由 20 提升为 128，适配长链路任务。

### 多模型接入体系重构（P0）

- **取消硬编码模型列表**：移除内置模型静态清单，模型列表基于服务商 Base URL 动态拉取（OpenAI 兼容协议 `/models` 接口）；拉取时机覆盖启动增量同步、服务商新增/编辑后立即拉取与设置页手动刷新按钮。
- **本地缓存与元数据兜底**：拉取结果持久化到本地缓存，离线或接口异常时自动使用缓存列表；接口未返回的模型元数据按统一默认值兜底；用户手动校准的模型元数据在重新拉取时按模型 ID 合并保留，不被覆盖。
- **服务商级管理**：新增服务商增/删/改能力（名称 + Base URL 必填，API Key 选填，可先建服务商后补密钥）；修改 Base URL 后自动重新拉取模型列表；预置 DeepSeek 服务商开箱即用（预置服务商可编辑、不可删除）。
- **按服务商独立密钥**：API Key 按服务商独立存储于 VSCode 密钥库（SecretStorage），互不干扰；存量全局密钥自动作为预置 DeepSeek 服务商的密钥兼容旧配置，平滑升级。
- **默认模型跟随**：默认模型不在新拉取的模型列表时自动跟随为该服务商首个可用模型，避免配置失效。

### 设置页模型配置 UI 重构（P0）

- **两级配置结构**：「模型配置」栏重构为「服务商管理区 + 模型参数配置区」两级结构——服务商管理区集中展示服务商列表（名称、Base URL、模型数、密钥状态、同步状态）并提供刷新/编辑/删除操作；模型参数配置区按所选服务商过滤展示模型列表与默认模型。
- **新增服务商弹窗**：独立弹窗承载服务商名称、Base URL（URL 格式校验）、API Key（选填、密码框 + 掩码显隐切换 + 清除密钥选项）。
- **底部模型切换下拉框分组**：对话面板底部模型切换下拉框按服务商分组展示（optgroup），多服务商场景下模型归属一目了然。

### 全量功能回归校验（P1）

- 智能体模式完整执行链路（文件读写、终端执行、多轮工具调用）、对话模式文件问答、上下文管理与 Token 实时统计、历史会话读写/续接/删除、编辑器文件修改高亮联动、多模型切换与配置管理、对话滚动交互与区块折叠、思考链分段展示与分层渲染等 V1.0.0 核心功能全量回归通过，本次重构仅作用于模型接入体系，无功能回归。

### 版本与文档迭代（P0）

- 版本号由 1.0.0 升级为 1.1.0，package.json 与 package-lock.json 全项目版本标识同步更新。
- 更新 CHANGELOG 与 README：重写多模型接入章节（服务商管理、动态拉取、按服务商密钥配置说明）；作者署名 wangh 保持不变。

## [1.0.0] - 2026-08-23

### 双层权限体系架构重构（P0）

- **「运行模式-权限管理」两级体系层级定义**：重新定义两层体系的从属关系与作用边界——第一层「运行模式」（对话模式/智能体模式）决定插件整体能力边界与控制可用工具集范围，是所有权限规则的前置判断条件；第二层「权限管理」（询问/全自动模式）属于智能体模式下的子配置，仅控制智能体模式中工作区内文件写入操作的确认机制，对话模式下不生效、无作用。
- **对话模式权限规则**：读取权限全域开放（工作区内所有文件 + 工作区外本地任意路径文件与目录结构）；写入权限全局禁用（无论工作区内/外，禁止任何文件增删改、重命名、新建目录等变更操作）；终端权限全局禁用（不发起任何系统命令调用）；仅挂载只读类工具（read_file / list_dir / search_code / get_diff）。
- **智能体模式权限规则**：读取权限全域开放（与对话模式一致，读取操作无需确认）；写入权限仅开放工作区范围内（工作区外文件强制只读，该限制不受权限管理模式影响）；终端权限保留完整能力，原有规则不变。
- **执行校验层模式感知化**：统一权限校验模块（checkToolAllowed）之外，文件写入判定（checkFileWrite）与终端命令判定（checkCommand）新增运行模式前置判断——对话模式下写入/终端操作在执行前直接拦截并返回统一提示文案，与调度层工具屏蔽形成双重防线；所有拦截同步写入操作审计日志。
- **权限配置项合并去重**：移除与「权限模式」重复、矛盾的「工作区内文件修改权限」配置项，权限管理统一由单一配置项控制，消除双配置互相矛盾造成的概念混淆；旧配置残留值自动失效，无副作用。
- **权限管理作用范围澄清**：询问模式（智能体模式下生效）——工作区内所有文件增删改操作执行前弹出确认面板（展示操作详情与 diff 预览），用户同意后方可执行，文件读取无需确认；全自动模式——工作区内文件增删改自主执行无需逐次确认，仍严格限制在工作区范围内，禁止越权修改工作区外文件。

### UI 控件与配置页逻辑对齐优化（P0）

- **底部快捷栏权限下拉框联动**：运行模式下拉框保持原有位置与交互不变；权限管理下拉框新增状态联动——切换至对话模式时置为禁用态（灰色不可点击），占位文案统一显示「🔒 对话模式·只读权限」；切换回智能体模式自动恢复可点击状态并展示当前选中的询问/全自动模式；悬停提示文案同步说明两层体系作用边界，消除概念混淆。
- **设置页权限配置项重构**：安全权限分组重构为「运行模式」与「权限管理」两个分组——「运行模式」分组承载插件启动时的默认运行模式（作用于全局）；「权限管理」分组以角标明确标注「仅智能体模式下生效」，承载文件写入确认方式（询问/全自动模式）与终端命令免确认、高危命令拦截等智能体专属权限项；每个配置项补充 tooltip 说明作用范围与生效条件。
- **UI 状态与底层规则完全一致**：快捷栏禁用态、设置页分组层级与底层权限判定逻辑一一对应，无展示与实际能力不匹配问题。

### 全量功能回归校验（P0）

- 智能体模式完整执行链路（文件读写、终端执行、多轮工具调用）、对话模式文件问答、上下文管理与 Token 实时统计、历史会话读写/续接/删除、编辑器文件修改高亮联动、多模型切换与配置管理、对话滚动交互与区块折叠、思考链分段展示与分层渲染等核心功能全量回归通过，权限修改仅作用于模式能力边界，未波及其他模块逻辑，无功能回归。

### 版本与文档迭代（P0）

- 版本号由 0.9.0 升级为 1.0.0（正式版本迭代，包含核心架构重构与稳定化交付），package.json 与 package-lock.json 全项目版本标识同步更新。
- 更新 CHANGELOG 与 README：重写运行模式与权限管理章节，明确两层体系的规则、边界与使用场景；作者署名 wangh 保持不变。

## [0.9.0] - 2026-08-23

### 对话模式权限体系修正（P0）

- **对话模式只读文件问答能力**：重新定义两种运行模式的权限边界——对话模式从「纯文本问答」升级为「只读文件问答」，完整开放只读类文件操作能力（文件内容读取、目录结构遍历、代码检索、diff 对比），支持读取工作区目录内所有文件，同时支持读取工作区外的本地文件与目录（含 @ 引用、右键注入、文件夹批量引用），满足基于项目文件与本地文件的问答需求。
- **调度层权限管控**：对话模式仅挂载只读工具（read_file / list_dir / search_code / get_diff），文件写入、终端执行等工具不入请求，从 Agent 调度层面屏蔽无效越权调用。
- **执行层统一权限校验**：工具执行前置链路新增统一权限校验模块（checkToolAllowed），基于当前运行模式判定操作合法性，所有工具调用必经该校验层；对话模式下任何写入/终端类操作均在执行前拦截并返回明确提示：「对话模式仅支持只读文件问答，如需修改文件或执行命令，请切换至智能体模式」，越权调用同步写入操作审计日志。
- **模式约束系统提示**：对话模式下向模型注入能力边界约束，明确告知仅只读工具可用、写入与命令操作会被系统拒绝，引导模型在需要修改代码或执行命令时提示用户切换模式。
- **智能体模式读取权限完整开放**：文件读取权限统一为「完整开放」——任何运行模式下均可读取工作区内所有文件及工作区外的本地文件与目录（文件读取、目录遍历、代码检索、diff 对比），工作区外读取不再按策略询问/拒绝（历史「工作区外文件策略」配置项随之废弃移除）。
- **询问/全自动模式作用范围收窄**：询问模式与全自动模式的区分仅作用于智能体模式下工作区内文件的修改/编辑/新增/删除；终端命令执行不再受该模式影响，仅由「终端免确认」开关与高危命令拦截规则控制。
- **工作区外写入禁令不变**：工作区外文件在任何运行模式下依旧只读——可读取、不可修改/删除，写入操作执行前拦截。
- **模式切换即时生效**：模式随每次请求实时传递，权限规则同步切换，无缓存残留。

### 核心功能全量回归校验（P0）

- 智能体模式完整执行链路、上下文管理与 Token 统计、历史会话读写、编辑器高亮联动、全局设置项配置、多模型切换、安全权限模式切换、对话滚动交互等核心功能全部回归通过，权限修改仅作用于对话模式能力边界，无功能回归。

## [0.8.0] - 2026-08-22

### 对话布局体系重构（P0）

- **双层滚动体系**：对话面板全局滚动 + 内容子块二级滚动——思考过程、工具执行结果、终端输出、工具参数等所有独立内容子块统一最大高度阈值（260px），超出后区块内部出现独立滚动条，长内容不再无限制撑高对话面板；子块滚动条样式对齐 VSCode 原生滚动条风格。
- **区块统一折叠/展开**：所有内容子块头部栏新增折叠切换按钮，可收起为单行标题条目（如「思考过程」「read_file · 执行成功」），默认展开；折叠不影响流式内容更新，折叠后形成清晰的执行链路条目列表，便于宏观把控完整任务流程。
- **滚动交互兼容**：子块内部滚动不触发全局滚动的自动归位逻辑，与 V0.7.0 智能滚动无交互冲突。

### 推理链路分层展示（P0）

- **推理节点（思维链主干）**：新增 insight 分段——工具调用轮次的模型正文（决策结论、动作说明、结果小结）作为轻量主干节点分散挂载到对应执行节点前后，工具调用出现时自动从回复气泡迁移；仅最终回复轮次的正文保留在正式回复气泡，收尾体验不变。
- **深度思考二级子块**：完整推理过程（reasoning_content）作为可折叠、可内部滚动的独立子块，挂载在对应推理节点附近，与主干节点视觉分层；严格遵循「推理节点 → 深度思考 → 工具调用执行 → 执行结果小结」时序结构，推理逻辑与每一步执行动作一一对应，完整思维链无缺失、不堆积。

## [0.7.0] - 2026-08-22

### 交互体验优化（P0）

- **对话滚动智能跟随**：重构输出过程中的滚动控制逻辑——用户手动拖动滚动条或滚动滚轮立即暂停自动跟随（视口稳定停留在当前位置，新内容不再强制拉回底部）；停止滚动操作 5 秒后自动平滑滚动至最新输出位置恢复实时进度展示，倒计时内再次滚动则重置计时；无用户干预时保持默认跟随，始终展示最新执行节点。
- **思考链分散分段排版**：思考区块内部按语义单元（空行）自动分段排版，段落间距与行高标准化，移除思考区内部滚动高度限制改为完整展开展示；所有分散思考区块沿用统一视觉样式，长思考内容支持独立展开/折叠（默认完整展开）。配合 V0.6.0 时序化分段结构，推理逻辑与每一步执行动作一一对应，任务结束后可完整回溯。

## [0.6.0] - 2026-08-22

### 交互范式重构（P0）

- **流式分步执行输出**：对话流渲染改为时序化分段结构——思考内容分段流式输出（每轮推理独立成段，可展开/折叠，默认展开），工具调用解析完整即插入工具执行节点（loading 态即时可见），执行结果完成后原位更新为结果节点；「思考→调用工具→返回结果→继续思考→调用下一工具」按实际执行时序逐段交替展示，任务结束后保留完整分步执行链路可回溯。旧历史消息自动回退原渲染方式，无兼容问题。

### 数据实时性优化（P1）

- **上下文 Token 实时统计**：每接收一段流式输出（思考/回复/工具结果）后节流（500ms）重算会话完整消息链 Token 占用，未落盘的流式内容按启发式估算补充计入，面板顶部进度条与数值随输出实时更新，无整轮结束才刷新的滞后。
- **今日用量与调用次数实时累加**：每次模型接口响应（每一轮流式调用）返回后立即累加今日 Token 用量、消费金额与 API 调用次数，与接口计费数据对齐；多轮任务执行过程中数据实时可查，杜绝收尾一次性补记的滞后。
- **账户余额全量字段展示**：余额查询全量映射官方接口字段（总余额/充值余额/赠金余额），并拼接本地已用金额与今日调用次数；设置面板打开时自动拉取最新用量数据，「查询账户余额」按钮支持手动刷新。

### 编辑器高亮联动修复（P1）

- **高亮样式精简**：新增行装饰移除 border 外边框，改为左侧 gutter 绿色竖线 + 浅绿行背景；删除行改为间隙前一行左侧红色竖线 + 浅红行背景（新增与删除重合时新增优先），对齐 VSCode 原生 Diff 视觉体验。
- **跳转联动即时生效**：点击对话框文件变更节点「跳转编辑器」打开文件时，自动重新应用最近一次变更的高亮装饰，打开即可看到增删行标记，无需关闭重开。
- **标记生命周期规范化**：同一文件再次修改时自动清除旧装饰刷新为最新变更；用户手动关闭文件后装饰类型释放，重新打开不再残留标记（仅通过跳转按钮打开时恢复展示）。

## [0.5.1] - 2026-08-22

### 缺陷修复（P0）

- **历史会话删除无响应修复**：删除操作改用两段式内联确认——点击「删除」后按钮变为「再点一次确认」，再次点击才真正删除，5 秒内未确认自动还原；修复根因为 VSCode webview 禁用原生 window.confirm（调用恒返回 undefined），原实现依赖其返回值判断导致删除消息从未发出。菜单关闭时同步清除确认态，避免状态残留。

## [0.5.0] - 2026-08-22

### 缺陷修复（P0）

- **会话列表空白修复**：修复历史会话元数据截断解析的尾随逗号问题（截断到 messages 字段前时上一字段逗号未移除，JSON 解析抛错），导致全部会话被误判损坏、侧边栏列表为空，新建会话也因文件监听触发的全量重扫而消失；修复后启动即恢复全部历史会话，新建会话实时入列。
- **账户已用金额统计修复**：重新实现已用金额口径——余额接口仅返回余额快照（赠金优先扣费场景下「充值+赠金−当前余额」恒为 0，无法推导累计消费），改为本地累计：每次模型调用完成后按「模型单价 × Token 用量」估算消费金额（DeepSeek 官方公开标准价表，自然日口径，持久化到本地用量日志）；设置页展示恢复为「余额 X.XX CNY（已用 Y.YY CNY）」，数值随调用消耗实时累加，接口异常仍展示「查询失败」兜底。
- **Token 统计口径修正**：无校准快照时（新会话/压缩后/重启后）的启发式估算中，CJK 字符由 1 token/字符修正为约 0.6 token/字符（对齐 DeepSeek 中文分词实测量级），避免中文会话上下文占用高估 60% 以上；压缩与重新生成后主动清除旧校准快照，防止链长重新增长后误用旧基准高估占用。

### 体验优化（P1）

- **会话列表虚拟滚动修复**：抽屉收起态挂载后展开时未建立高度测量的问题修复，展开后可视窗口正常计算（此前退化为单行窗口）；长会话列表滚动恢复流畅。
- **外部点击收起交互修正**：收起例外选择器与侧边栏实际类名对齐，展开态下侧边栏内部点击不会误触发收起。

## [0.4.0] - 2026-08-22

### 性能优化（P0，执行效能专项落地）

- **并行工具调用**：同批次工具调用按依赖关系分组调度——连续只读工具（read_file/list_dir/search_code/get_diff）合并并行执行，连续 write_file 目标路径互不相同可并行，execute_command 恒串行保持命令语义与顺序依赖；多文件读取、目录检索等批处理场景工具执行段由串行叠加降为单次最大耗时。
- **流式执行流水线**：模型流式输出过程中，单个工具调用参数接收完整（JSON 闭合可解析）即预启动只读工具执行，推理/思考生成与工具执行并行重叠；流结束后直接复用预执行结果（未完成则等待，不重复执行），最终未生效的调用结果自然丢弃。
- **上下文动态裁剪**：距离消息链末尾超过 16 条且内容超 4000 字符的历史工具输出，在发送前轻量降级为「头部结论 + 省略标记 + 尾部细节」（保留文件头/命令头与错误信息等关键信息）；仅发送时降级，本地持久化完整原文不变，保障回溯与自我修正能力。
- **渲染层增量更新**：MessageBubble 与 StepBlock 组件 memo 化（消息引用/步骤引用不变时跳过重渲染），流式期间仅目标消息与目标步骤更新，长会话高频更新时 UI 线程占用下降。

## [0.3.0] - 2026-08-22

### 缺陷修复（P0）

- **Token 统计真实化**：移除文件大小/词元计数等近似估算，改用模型返回的真实 `prompt_tokens` 值校准（消息链长度快照对齐统计口径，链收缩时回退启发式估算）；顶部栏展示统一为「已用 X.XK / 总窗口 Y.YK」格式，会话 Token 统计与用量上报口径一致。
- **账户已用金额修复**：DeepSeek 余额接口无直接「已用」字段，改为差额法计算（已用 = 充值 + 赠送 − 总余额），修复已用金额被误映射为赠送余额的问题；接口异常时展示独立错误提示，不再显示错误金额。
- **历史会话持久化加载异常修复**：激活 + WebView 就绪后强制全量扫描；严格匹配会话文件命名过滤日志文件（audit-log.jsonl / usage-log.json 等不再误入列表）；仅解析元数据不全量加载；单文件损坏记录错误日志并跳过；新增历史目录变更监听（防抖 300ms）实时同步列表。

### 功能增强（P1）

- **编辑器 diff 装饰与跳转联动**：文件修改完成后自动在编辑器内高亮变更行（新增行绿色背景 + 删除行红色竖条），关闭文件自动清除装饰；对话框内 diff 全量渲染改为「文件名 + 变更统计」精简节点，点击跳转编辑器定位首处变更。
- **编辑器右键选中代码注入**：编辑器右键菜单新增「Add to Code Agent」（有选区时可用），将选中代码以行范围引用注入当前对话输入框（展示 `文件名 起始行-结束行` 标签），仅将对应行号范围的代码加载至上下文，与 @ 全文件引用复用同一套上下文管理机制；面板未就绪时请求暂存，就绪后自动下发。
- **思考语言自适应**：系统提示词按用户提问语言动态约束思考过程与回复语言（中文提问→中文思考，英文提问→英文回复），保留技术术语原文。
- **工具调用最大轮次可配置**：设置页「Agent 行为」分组新增「最大工具调用轮次」数值项（正整数 1-1000，默认 20，非法输入自动回退合法值）；配置即时生效（新建与续接会话无需重启）；触发上限时提示文案展示当前配置值并引导调整入口。

### 体验优化（P1）

- **会话侧边栏点击外部自动收起**：侧边栏展开时点击对话主面板空白/内容区域自动收起（输入框、按钮等交互控件不触发），收起动画与原有收起按钮一致，布局状态同步持久化。

## [0.2.0] - 2026-08-21

### 缺陷修复（P0）

- **上下文压缩消息链非法导致 400 错误**：压缩切分边界自动对齐（保留段开头不再出现 tool 消息），发送前增加消息链合法性前置校验（剔除孤立 tool 消息、未完整配对的 tool_calls 整体降级为占位文本），杜绝压缩后请求被 API 拒绝。
- **WebView 冷启动偶发空白/无响应**：新增 ready 握手与 boot:ack 就绪确认机制；渲染失败自动重试 1 次；超时展示错误提示界面并提供手动刷新入口；设置面板与权限弹窗改为按需懒加载，显著降低冷启动开销。

### 功能增强（P1）

- **会话列表体验**：
  - 虚拟滚动：100+ 会话列表流畅滚动；
  - 右键菜单标准化：菜单位置对齐鼠标坐标，点击外部/ESC 关闭，重命名失焦自动提交；
  - 抽屉式展开/收起：展开不挤压对话区域，右侧拖拽调宽（180-480px），收起无残留占位，布局状态持久化。
- **思考过程展示**：DeepSeek 推理模型 `reasoning_content` 全链路流式展示；思考区块视觉弱化，生成中自动展开、完成后默认折叠，内容永久保留可回溯。
- **Markdown 渲染增强**：完整表格渲染；代码块语法高亮（12+ 语言）、行号、一键复制；富文本复制（剪贴板同时写入纯文本与 Markdown 源文本，粘贴到编辑器保留格式）。
- **权限模式快捷切换**：底部栏新增询问/全自动模式切换器，即时生效并与全局设置双向同步；全自动模式仅放行常规操作（工作区内文件写入、工作区外读取、工作区内命令），高危命令始终强制二次确认。
- **模型参数语义明确化**：模型元数据（上下文窗口 / 最大输出 Token = 模型能力上限，用于边界校验与用量统计）与全局推理参数 `max_tokens`（单次请求生成长度限制 = 运行时参数）tooltip 区分；设置面板实时校验：`max_tokens` 超过所选模型输出上限时警示并可一键修正，保存与请求阶段均自动截断为上限值；模型编辑校验输出上限不得超过上下文窗口。
- **API Key 安全展示**：密文显示 + 显隐切换按钮 + 实时配置状态反馈（已配置/未配置/待保存）。

### 性能优化（P1）

- **对话区虚拟滚动**：仅渲染视口内消息区块（动态高度测量 + 窗口切片 + 预估高度），流式生成自动跟随滚动；百轮长会话打开、滚动、发送均流畅，DOM 节点数量恒定于视口规模。
- **长内容默认折叠**：超长代码块（>30 行）与超长纯文本（>4000 字符）默认折叠，点击展开全文，避免一次性渲染大量 DOM 节点。
- **数据层惰性加载**：会话列表仅读取文件头部元数据（截断解析，不解析消息体）；完整消息体在打开会话时按需读取；内存缓存 LRU 上限 8 个会话，非激活数据不常驻内存；列表广播走内存元数据缓存，零磁盘 IO。
- **通信层消息合并**：流式 chunk 与思考增量 16ms 缓冲批量下发，显著降低高频 postMessage 对主线程的阻塞。

## [0.1.0]

- 首个正式版本：多模型接入（OpenAI 兼容协议）、智能编程 Agent（任务规划/工具调度/多轮闭环）、四层上下文管理与自动压缩、@ 文件引用、会话管理、安全权限体系与操作审计。
